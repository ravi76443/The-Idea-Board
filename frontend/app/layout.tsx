import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Idea Board - Share Your Brilliant Ideas',
  description: 'A collaborative platform for sharing and voting on innovative ideas. Transform your thoughts into reality with community support.',
  keywords: 'ideas, innovation, collaboration, voting, brainstorming, startup ideas',
  authors: [{ name: 'Idea Board Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  )
}
